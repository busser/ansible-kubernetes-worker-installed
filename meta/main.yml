galaxy_info:
  author: Arthur Busser
  description: An Ansible role that makes a node a Kubernetes Worker.

  license: Unlicense

  min_ansible_version: 2.0

  platforms:
  - name: CentOS
    versions:
    - 7

  galaxy_tags:
  - kubernetes
  - kubeadm
  - worker

dependencies:
- role: kubeadm-installed
  kubeadm_version: "{{ kubernetes_version }}"
